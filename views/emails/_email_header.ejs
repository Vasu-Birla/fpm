<!DOCTYPE html>
<html>
<head>
  <meta charset="UTF-8">
  <title><%= title || (org && org.name) || 'Notification' %></title>
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <style>
    /* ---- Base ---- */
    body {
      margin: 0;
      padding: 0;
      background-color: #0f172a; /* slate-900 */
      font-family: -apple-system, BlinkMacSystemFont, "Segoe UI", Arial, sans-serif;
      -webkit-font-smoothing: antialiased;
    }
    .preheader {
      display: none !important;
      visibility: hidden;
      opacity: 0;
      color: transparent;
      height: 0;
      width: 0;
      max-height: 0;
      max-width: 0;
      overflow: hidden;
      mso-hide: all;
    }

    /* ---- Layout ---- */
    .email-outer {
      width: 100%;
      background-color: #0f172a;
      padding: 28px 0;
    }
    .email-inner {
      width: 100%;
      max-width: 640px;
      margin: 0 auto;
      background: #0b1120; /* darker shell behind card */
      border-radius: 20px;
      box-shadow: 0 18px 45px rgba(15,23,42,0.85);
      padding: 1px;
    }
    .email-card {
      background: #ffffff;
      border-radius: 18px;
      overflow: hidden;
    }

    /* ---- Card header ---- */
    .email-card-header {
      padding: 18px 24px 16px 24px;
      border-bottom: 1px solid #e5e7eb;
      background: radial-gradient(circle at top left, #e0edff 0, #ffffff 55%);
    }
    .header-row {
      display: flex;
      align-items: center;
      justify-content: space-between;
      gap: 12px;
    }
    .brand-left {
      display: flex;
      align-items: center;
      gap: 10px;
    }
    .brand-logo-wrapper {
      width: 40px;
      height: 40px;
      border-radius: 999px;
      background: #e5edff;
      display: flex;
      align-items: center;
      justify-content: center;
    }
    .brand-logo {
      max-width: 46px;
      max-height: 46px;
      display: block;
    }
    .brand-text-main {
      font-size: 15px;
      font-weight: 600;
      color: #0f172a;
    }
    .brand-text-sub {
      font-size: 11px;
      color: #6b7280;
      margin-top: 1px;
    }
    .badge-context {
      font-size: 11px;
      color: #4b5563;
      padding: 4px 10px;
      border-radius: 999px;
      background: #e5e7eb;
      text-transform: uppercase;
      letter-spacing: .08em;
      font-weight: 600;
      white-space: nowrap;
    }

    .header-title-block {
      margin-top: 14px;
      text-align: left;
    }
    .header-title {
      font-size: 20px;
      font-weight: 600;
      color: #111827;
      margin: 0;
    }
    .header-subtitle {
      font-size: 13px;
      color: #6b7280;
      margin-top: 4px;
    }

    /* ---- Content ---- */
    .email-card-body {
      padding: 22px 24px 18px 24px;
      font-size: 15px;
      line-height: 1.6;
      color: #374151;
    }
    .email-title {
      font-size: 18px;
      font-weight: 600;
      color: #111827;
      margin: 0 0 4px 0;
    }
    .email-lead {
      font-size: 14px;
      color: #4b5563;
      margin: 0 0 16px 0;
    }
    .text {
      font-size: 14px;
      color: #374151;
      margin: 0 0 10px 0;
    }

    /* ---- Special blocks ---- */
    .otp-wrapper {
      text-align: center;
      margin: 18px 0 16px 0;
    }
    .otp-label {
      font-size: 12px;
      letter-spacing: 0.1em;
      text-transform: uppercase;
      color: #6b7280;
      margin-bottom: 6px;
    }
    .otp-pill {
      display: inline-block;
      padding: 12px 26px;
      border-radius: 999px;
      background: linear-gradient(135deg, #004aad, #1d4ed8);
      color: #ffffff;
      font-weight: 700;
      letter-spacing: 0.25em;
      font-size: 20px;
      box-shadow: 0 10px 25px rgba(37,99,235,0.4);
    }

     .otp-box { background:#004aad; color:#fff; font-size:26px; font-weight:bold; padding:15px; margin:20px auto; border-radius:5px; display:inline-block; letter-spacing:3px; }

    .info-box {
      margin: 16px 0 8px 0;
      padding: 12px 14px;
      border-radius: 10px;
      border: 1px solid #e5e7eb;
      background: #f9fafb;
      font-size: 13px;
      color: #4b5563;
    }
    .info-box strong {
      color: #111827;
    }

    .meta-note {
      font-size: 12px;
      color: #9ca3af;
      margin-top: 14px;
    }

    /* ---- Button (for other emails, if needed) ---- */
    .btn-primary {
      display: inline-block;
      padding: 11px 22px;
      border-radius: 999px;
      background: linear-gradient(135deg, #004aad, #1d4ed8);
      color: #ffffff !important;
      font-weight: 600;
      font-size: 14px;
      text-decoration: none;
      margin: 14px 0;
      box-shadow: 0 12px 30px rgba(37,99,235,0.45);
    }

    /* ---- Footer ---- */
    .email-card-footer {
      padding: 14px 24px 18px 24px;
      border-top: 1px solid #e5e7eb;
      background-color: #f9fafb;
      font-size: 11px;
      color: #9ca3af;
      text-align: center;
    }
    .email-card-footer a {
      color: #6b7280;
      text-decoration: none;
    }
    .email-card-footer a:hover {
      text-decoration: underline;
    }

    @media only screen and (max-width: 640px) {
      .email-inner {
        border-radius: 0;
      }
      .email-card {
        border-radius: 0;
      }
      .email-card-header,
      .email-card-body,
      .email-card-footer {
        padding-left: 18px !important;
        padding-right: 18px !important;
      }
      .header-row {
        flex-direction: row;
        align-items: center;
      }
      .brand-text-main {
        font-size: 14px;
      }
      .email-title {
        font-size: 17px;
      }
    }
  </style>
</head>
<body>
  <!-- Hidden preheader text (inbox preview) -->
  <div class="preheader"><%= preheader || '' %></div>

  <div class="email-outer">
    <div class="email-inner">
      <div class="email-card">
        <div class="email-card-header">
          <div class="header-row">
            <div class="brand-left">
              <div class="brand-logo-wrapper">
                <% if (logoSrc) { %>
                  <img src="<%= logoSrc %>"
                       alt="<%= (org && org.name) || 'Logo' %>"
                       class="brand-logo" >
                <% } else { %>
                  <span style="font-size:20px;color:#1d4ed8;">⚖️</span>
                <% } %>
              </div>
              <div>
                <div class="brand-text-main">
                  <%= (org && org.name) || 'Our Service' %>
                </div>
                <div class="brand-text-sub">
                  Secure legal & client communications
                </div>
              </div>
            </div>
            <div>
              <div class="badge-context">
                <%= (title || 'Notification') %>
              </div>
            </div>
          </div>

   
        </div>

        <!-- CONTENT START -->
        <div class="email-card-body">
